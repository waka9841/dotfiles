# Public dotfiles SSH config skeleton.
# Internal host definitions live in ~/.ssh/config.d/*.conf (gitignored).

Include ~/.ssh/config.d/*.conf

# Colima
Include ~/.colima/ssh_config

Host *
  ForwardAgent yes
  ServerAliveInterval 60
  ServerAliveCountMax 3
